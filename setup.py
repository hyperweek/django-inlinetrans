from setuptools import setup, find_packages

setup(
    name='django-inlinetrans',
    version='0.1.0',
    description='Django-inlinetrans is a django application that allows the translation of django templates from the rendered html in the browser.',
    author='dnordberg',
    author_email='dnordberg@gmail.com',
    url='http://github.com/dnordberg/django-inlinetrans/tree/master',
    packages=find_packages(),
)
